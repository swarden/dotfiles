stages:
- name: Build
  steps:
  - runScriptConfig:
      image: base/archlinux
      shellScript: |-
        pacman -Syy
        pacman -S base-devel --noconfirm
        cd dwm
        ls -lisah
timeout: 60
branch:
  include:
  - master
  exclude:
  - PR*
